<div class="flat-conteneur">
  <div class="pictures-conteneur">
    <div class="picture-principale">
      <p>Picture here...</p>
    </div>
    <div class="picture-grille">
      <div class="picture-left-top">
        <p>Picture here...</p>
      </div>
      <div class="picture-right-top">
        <p>Picture here...</p>
      </div>
      <div class="picture-left-down">
        <p>Picture here...</p>
      </div>
      <div class="picture-right-down">
        <p>Picture here...</p>
      </div>
    </div>
  </div>
  <div class="bottom-conteneur">
    <div class="description-card">
      <div class="section-description">
        <h2><%= @flat.name %>   <%= link_to flat_favorites_path(@flat), data: {turbo_method: :post} do %>
            <i class="fa-regular fa-heart fa-xl" style="color: #25316d;"></i>
          <% end %>
        </h2>
      </div>
      <div class="section-description">
        <i class="fa-solid fa-star fa-lg" style="color: #e6c700;"></i>
        <p><strong>4.8 en moyenne</strong></p>
      </div>
      <div class="section-description">
        <i class="fa-solid fa-location-pin fa-lg" style="color: #25316d;"></i>
        <p><%= @flat.location %></p>
      </div>
      <div class="section-description">
        <i class="fa-solid fa-house fa-lg" style="color: brown;"></i>
        <p>Hébergé par <i><%= @flat.user.first_name %></i></p>
      </div>
      <div class="section-description">
        <p><%= @flat.description %></p>
      </div>
      <div class="section-description">
        <i class="fa-solid fa-battery fa-lg" style="color: green;"></i>
        <p><strong>Autonomie : <%= @flat.autonomy_level %>/5</strong></p>
      </div>
      <div class="section-description">
        <p><i class="fa-solid fa-user fa-lg" style="color: brown;"></i> À propos de <i><%= @flat.user.first_name %></i> : Le Lorem Ipsum est simplement du faux texte employé dans la composition et la mise en page avant impression. Le Lorem Ipsum est le faux texte standard de l'imprimerie depuis les années 1500, quand un imprimeur anonyme assembla ensemble des morceaux de texte pour réaliser un livre spécimen de polices de texte. (biographie)</p>
      </div>
      <div class="section-description">
        <i class="fa-solid fa-map fa-lg" style="color: blue;"></i>
        <div class="map-conteneur">
          <div class="map-display" data-controller="map" data-map-markers-value="<%= @markers.to_json %>" data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
        </div>
        </div>
        <% @flat.bookings.each do |booking| %>
          <% booking.reviews.each do |review| %>
            <%= review.content %>
            <%= review.rating %>
          <% end %>
        <% end %>
      </div>

    <div class="book-card">
      <div class="section-book">
        <h4><strong><%= @flat.price_per_month %>€ / mois</strong></h4>
        <% @booking = Booking.new %>
        <%= simple_form_for [@flat, @booking] do |f| %>
          <%= f.input :start_date,
              label: "Du :",
              as: :string,
              input_html: { data: { controller: "datepicker" } } %>
          <%= f.input :end_date,
              label: "Au :",
              as: :string,
              input_html: { data: { controller: "datepicker" } } %>
          <div class="d-flex justify-content-center">
          <%= f.submit "Book", class: "btn btn-outline-dark" %>
          </div>
        <% end %>
      </div>
    </div>
    </div>
  </div>
</div>

<%= link_to "Edit", edit_flat_path(@flat) %>
